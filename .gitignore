# en-GB: Excludes generated and local-only artefacts so commits contain intentional source changes.
node_modules
# Keep local environment variants out of version control while preserving the safe template
.env*
!.env.example

/generated/prisma
.next
apps/web/.next
dist
.shiftflow
coverage
*.tsbuildinfo
# .NET build and test output is generated locally and in CI.
**/bin/
**/obj/
TestResults/
