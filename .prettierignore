# en-GB: Keeps local-only Markdown records out of formatting checks so the public gate evaluates repository files only.
prompts/**/*.md
docs/history/*.md
# .NET source and lock files are formatted by the .NET toolchain.
apps/api-dotnet/**
# Preserve the hash-pinned SPDX 2.3 schema bytes exactly as vendored.
eng/spdx-2.3-schema.json
