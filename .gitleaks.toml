# en-GB: Configures  gitleaks rules so automated security checks remain deterministic.
[allowlist]
description = "Ignore generated local artifacts that are already excluded from Git."
paths = [
  '''(^|/)apps/web/\.next/''',
  '''(^|/)dist/''',
  '''(^|/)generated/''',
  '''(^|/)node_modules/'''
]
